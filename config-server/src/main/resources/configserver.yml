spring:
  application:
     name: config-server
  cloud:
    config:
      server: 
        git:
          uri:  https://github.com/chjohnson-pivotal/pcf-auto-simulator
          searchPaths: config
 
server:
  port: 8888